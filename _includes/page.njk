<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <header>
    {% block header %}
    <nav>
      <ul>
        {% for mainPage in collections.main %}
        <li {% if page.url == mainPage.url %} aria-current="page"{% endif %}>
          <a href="{{ mainPage.url }}">
            {{ mainPage.data.title }}
          </a>
        </li>
        {% endfor %}
      </ul>
    </nav>
    {% endblock %}
  </header>
  <main>
    {% block main %}
    Content
    {% endblock %}
  </main>
  <footer>
    {% block footer %}
      <ul>
        {% for link in social %}
          <li>
            <a href="{{ link.link }}" target="_blank">
              {{ link.title }}
            </a>
          </li>
        {% endfor %}
      </ul>
    {% endblock %}
  </footer>
</body>
</html>
